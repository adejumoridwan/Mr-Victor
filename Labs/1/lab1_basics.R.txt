x = 1 + 1
y = 2 * x
z <- (x + y)

#  This is a comment describing that below we are computing
#  the square root of 4
sqrt(4)

sin(pi/2)^2

(3 * 2^2 - 4 * 3^2 )*cos(3/4*pi) 

x <- 3 + 2 * 3 # storing result as 'x'
x
some_new.name <- x + 11 # reusing 'x'
some_new.name


log(1) # using default value of 'base' argument
log(2, base=2) # specifying value of 'base' by name
log(2, 10) # specifying value of 'base' by order


rm(x)

rm(list=ls())


weight <- c(60,72,57,90,95) # Numerical vector
weight
height <-c(1.75, 1.80, 1.65, 1.90, 1.74)
bmi <- weight/height^2
bmi


friends<-c('Lucy','Andrew',
		'Tim',"Charlie","Bob") #  A vector of text strings
girls<-c(TRUE,FALSE,FALSE,
		FALSE, FALSE) #  A vector of logical values

class(weight)
class(friends)
class(girls)

wnamed<-c(a=60,sec=72,Bill=4) 
wnamed



friends == 'Tim'

length(weight) 
sum(weight)
# do you remember the mean ?
xbar <- sum(weight)/length(weight)
xbar

any(weight < 70)
all(weight < 70)
which(weight < 70)
weight %in% 70

names(wnamed)

as.character(weight)

seq(2,5,length=4)
2:5
rep(2,5)

weight[3]
weight[-3]

wnamed['a']

height[weight == 72]

weight
weight[c(2,3)]

wnamed[c('a','Bill')]

height[weight != 72]

# 
v <- c(1, 2, NA, 3)
mean(v)

is.na(v)

newlist <- list(first = "hello", second = 42, Bob = TRUE) 
is.list(newlist)
newlist
newlist[[2]]
newlist$Bob

matrix(1:12,nrow=3)

matrix(1:12,nrow=3,byrow=TRUE)

z <- matrix(1:12,nrow=3,byrow=TRUE)
rownames(z) <- LETTERS[1:3]

z
t(z)

z[1,2]   # element in 1st row, 2nd column
z[3,]    # 3rd row
z[,4]    # 4th column
z[1:2,3:4] # elements in the first two rows and in the 3th and fourth columns
diag(z)  # main diagonal

bmifun <- function(x, y)
{
  bmi <- x / y^2
  return(bmi)
}

bmifun(weight,)

n<-10
x<-(-n):n
y<-x^2
plot(x,y)

plot(x,y,xlab="x data",ylab="y data",type="l", 
   col="blue",lwd=3,main="Plot of X vs. Y")


z <- x^3/10 
xrange <- range(x) 
yrange <-range(y,z) 
plot(x,y,xlab="x data",ylab="y or z",type="l",
 col="blue",lwd=3,xlim=xrange,ylim=yrange) 
par(new=TRUE) 
plot(x,z,xlab="",ylab="",type="l",axes=FALSE, 
col="red",lwd=3,xlim=xrange,ylim=yrange,lty="dashed")
legend("bottomright",legend=c("y","z"),
col=c("blue","red"),lwd=2,cex=2)

par(mfcol=c(1,2)) 
plot(x,y,xlab="x data",ylab="y",type="l", 
    col="blue",lwd=3) 
abline(v=0,lwd=2,lty="dashed") # draw a vertical
                                   # line at 0
plot(x,z,xlab="x data",ylab="z data",type="b",
	 col="red",lwd=3,pch=19) 
abline(h=0,lwd=2,lty="dashed") # draw a horizontal line at 0

curve(sin(x)/x, from=-2*pi,to=2*pi)
curve(cos(x)/5, from=-2*pi,to=2*pi,add=TRUE,lty=2)
legend("topright",legend=c("sin(x)/x","cos(x)/5"),
   lty=1:2)

x <- seq(-6400, 6400, length= 60) 
y <- x 
f <- function(x, y) { 
     sqrt(6400^2-(x^2+y^2)) 
}
z <- outer(x, y, f) 
z[is.na(z)] <- 1

persp(x, y, z, theta = 30, phi = 30,
	 expand = 0.5, col = "lightblue")

image(x, y, z)

contour(x, y, z)

image(x, y, z)
contour(x, y, z,add=TRUE)

filled.contour(x, y, z)


install.packages(plot3D)
par(mfrow=c(2,2))
library(plot3D)
X       <- seq(0, pi, length.out = 50)
Y       <- seq(0, 2*pi, length.out = 50)
M       <- mesh(X, Y)
phi     <- M$x
theta   <- M$y

# x, y and z grids
r <- sin(4*phi)^3 + cos(2*phi)^3 + sin(6*theta)^2 + cos(6*theta)^4
x <- r * sin(phi) * cos(theta)
y <- r * cos(phi)
z <- r * sin(phi) * sin(theta)
# full colored image
surf3D(x, y, z, colvar = y, colkey = FALSE, shade = 0.5,
       box = FALSE, theta = 60)
# same, but just facets
surf3D(x, y, z, colvar = y, colkey = FALSE, box = FALSE, 
       theta = 60, facets = FALSE)
# with colors and border, AND increasing the size
# (by reducing the x- y and z- ranges
surf3D(x, y, z, colvar = y, colkey = FALSE, box = FALSE, 
       theta = 60, border = "black", xlim = range(x)*0.8, 
       ylim = range(y)*0.8, zlim = range(z)*0.8)
# Now with one color and shading
surf3D(x, y, z, box = FALSE,
       theta = 60, col = "lightblue", shade = 0.9)

ImageOcean(col = "white",	contour = list(levels = seq(-6000, 0, by = 2000)))
